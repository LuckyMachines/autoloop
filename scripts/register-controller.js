const hre = require("hardhat");

async function main() {
  // register controller with registrar contract
}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
